#cmake -G "Unix Makefiles" -B ./build
PROJECT(main)
CMAKE_MINIMUM_REQUIRED(VERSION 3.16.0) 
ADD_SUBDIRECTORY( ber )
AUX_SOURCE_DIRECTORY(. DIR_SRCS)
ADD_EXECUTABLE(main ${DIR_SRCS})
TARGET_LINK_LIBRARIES(main BER_LIB)